// const fruits: string[] = ["Apple", "Banana", "Grape"]

// Tuple（タプル）型 厳密に個別指定
const book: [string, number, boolean] = ["Buziness", 2500, false];
book.push(2000) // 型に無い追加は一旦許容
// const fruit = fruits[fruits.length - 1]

// console.log(book[3]) // reading時にエラー指定
